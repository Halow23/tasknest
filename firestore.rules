rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The TaskNest API server is the only data gateway; it uses the Admin SDK,
    // which bypasses security rules entirely. Clients get nothing directly.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
