# Publication Synchronization Notes

- The latest shared preview shows the Foundation University account-required sign-in screen.
- The published `tasknest-mrafqspx.manus.space` domain still serves the older generic TaskNest sign-in screen after checkpoint `269810b1`.
- The production-log endpoint returned `cloudrun service not found`, so the managed live deployment could not be inspected or reconciled from this workspace.
