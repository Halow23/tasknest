# TaskNest Supporting Asset Placement Notes

The wave field is right-weighted and carries its clearest blue contour detail in the upper-right composition, so the dashboard header uses a larger, top-right crop with a low-opacity right-side gradient.

The collaboration orbit is right/bottom weighted with its ring, blue nodes, and coral signal concentrated there, so the team-context card anchors the artwork at the lower-right and preserves contrast for its text on the left.

The calendar sculpture already reads clearly in the workspace orientation ribbon, where its compact geometric volume supports calendar context without competing with project data.
